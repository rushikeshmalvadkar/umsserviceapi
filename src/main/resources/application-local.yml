spring:
  jpa:
    properties:
      hibernate:
        generate_statistics: true
        format_sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/umsdatabase
    username: root
    password: root

logging:
  level:
    'com.rm.ums' : debug
    'org.hibernate.stat' : debug
    'org.hibernate.SQL' : debug
    'org.hibernate.orm.jdbc.bind' : trace